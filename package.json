{
  "name": "restaurant-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github-yunemak:yunemak/restaurant-page.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "css-loader": "^7.1.4",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.7",
    "style-loader": "^4.0.0",
    "webpack": "^5.108.4",
    "webpack-cli": "^7.2.1",
    "webpack-dev-server": "^6.0.0"
  },
  "scripts": {
    "build": "webpack",
    "dev": "webpack serve",
    "deploy": "git branch gh-pages; git checkout gh-pages && git merge main --no-edit; git subtree push --prefix dist origin gh-pages; npx webpack; git add dist -f && git commit -m 'Deployment commit'; git subtree push --prefix dist origin gh-pages; git checkout main"
  }
}
